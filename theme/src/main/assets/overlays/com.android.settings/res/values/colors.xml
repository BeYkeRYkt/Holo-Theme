<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dashboard background color -->
    <color name="dashboard_category_background_color">@android:color/transparent</color>
    <color name="dashboard_background_color">@android:color/transparent</color>

    <color name="theme_accent">@*android:color/holo_primary</color>

    <!-- SwitchBar -->
    <color name="switchbar_background_color">@android:color/transparent</color>

    <drawable name="expanded_item_bg">@android:color/transparent</drawable>

    <!-- Lockscreen shortcuts -->
    <color name="lockscreen_shortcuts_text_color">@color/theme_accent</color>
    <color name="lockscreen_shortcuts_tint_color">@color/theme_accent</color>

    <!-- Multi-sim Color Picker Text Color -->
    <color name="msim_color_picker_text_color">@color/theme_accent</color>

    <!-- Multi-sim Edit Name Text Color -->
    <color name="msim_edit_name_text_color">@color/theme_accent</color>
    <color name="factory_reset_color">#FFFE412C</color>

    <!-- Lockscreen Pattern-->
    <color name="lock_pattern_view_regular_color">@color/theme_accent</color>
    <color name="lock_pattern_view_success_color">#ff009688</color>
    <color name="lock_pattern_view_error_color">#fff4511e</color>

    <color name="unlock_pattern_view_regular_color">@color/theme_accent</color>
    <color name="unlock_pattern_view_success_color">#ff009688</color>
    <color name="unlock_pattern_view_error_color">#fff4511e</color>

    <!-- Battery Menu (CM12) -->
    <color name="text_color_black">@color/theme_accent</color>
    <color name="text_color_darker_gray">@color/theme_accent</color>
    <color name="regulatory_text_color">@color/theme_accent</color>
    <color name="text_color_secondary_text_material_light">@color/theme_accent</color>

    <!-- QS Tile Re-arrange Screen -->
    <!-- <color name="qs_tile_default_background_color">#ff161616</color> -->
    <color name="qs_tile_tint_color">#ffe6e6e6</color>
    <color name="background_color_gray">@drawable/preference_background</color>
    <color name="cardview_light_background">#353535</color>
    <color name="cardview_dark_background">#353535</color>

    <!-- Security billing -->
    <color name="security_settings_billing_desc_color">@color/theme_accent</color>

    <!-- Fab Shape -->
    <color name="fab_shape">@*android:color/holo_primary</color>

    <!-- Owner Info Background -->
    <color name="background_color_white">@android:color/transparent</color>

    <!-- Category text color -->
    <color name="category_text_color">@*android:color/secondary_text_holo_dark</color>
</resources>
