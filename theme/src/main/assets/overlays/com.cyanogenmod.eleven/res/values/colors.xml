<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Material -->
    <color name="primary">#ff0d0d0d</color>
    <color name="primary_dark">@*android:color/black</color>
    <color name="accent">@*android:color/holo_blue_light</color>

    <color name="semitransparent_black">@color/primary</color>

    <!-- Standard Text Color -->
    <color name="default_text_color">@*android:color/holo_primary</color>
    <color name="default_text_color_light">#bfffffff</color>

    <!-- Cards -->
    <color name="cards_bg_color">@color/primary</color>

    <color name="background_color">@android:color/black</color>

    <!-- Color for the action bar -->
    <color name="header_action_bar_color">@color/primary</color>
    <color name="header_action_bar_text_color">@color/accent</color>
    <color name="header_action_bar_text_color_active">@color/accent</color>

    <!-- Background color used on some page headers -->
    <color name="page_header_background">@color/background_color</color>

    <!-- Color for background for shadow on playlist page -->
    <color name="header_shadow_color">@*android:color/transparent</color>

    <!-- List view related colors -->
    <color name="smart_playlist_item_background">#1931353f</color>
    <color name="list_item_background">#c1000000</color>
    <color name="list_item_header_text_color">@color/default_text_color</color>
    <color name="list_item_search_footer_text_color">#ff41a4f4</color>
    <color name="list_item_text_color">@color/default_text_color</color>
    <color name="list_item_text_color_light">@color/default_text_color_light</color>
    <color name="list_item_divider_color">@color/list_item_divider_color_light</color>
    <color name="list_item_divider_color_light">#34ffffff</color>
    <color name="grid_list_item_text_color">@color/default_text_color</color>

    <!-- Top title page colors -->
    <color name="tab_ripple_color">@color/accent</color>
    <color name="tab_accent_color">@color/accent</color>
    <color name="tab_selected_underline_color">@color/accent</color>

    <!-- Color for the Progress bar -->
    <color name="circular_progress_bar_background">#40ffffff</color>
    <color name="circular_progress_bar">@color/accent</color>

    <!-- Letter tile colors -->
    <color name="letter_tile_default_color">@color/primary</color>

    <!-- Color for the visualizer bars -->
    <color name="visualizer_fill_color">@color/accent</color>

    <color name="background_toolbar">@color/primary</color>
    <color name="background_main_toolbar">@color/primary</color>
</resources>